---
date: 2026-07-19
tags: [meta, git]
---

# 2026-07-19

## What I learned
My Goal is to finish this week with the following tasks suggest to me by claude:
WEEK 1: JS Core Mechanics

Day 1:  Values, types, operators
Read: EJS Ch.1 (Values, Types, Operators)
Quiz: What's the difference between null and undefined? What does NaN === NaN return? What's short-circuit evaluation?
Problems: Write a function checking if a number is even/odd without %. Predict output of 5 type-coercion snippets (e.g. "5" + 3, "5" - 3).

Day 2: Program structure, control flow
Read: EJS Ch.2 (Program Structure)
Quiz: Difference between let, const, var? What's a block scope?
Problems: FizzBuzz. Write a loop that sums all numbers divisible by 3 or 5 under 1000.

Day 3: Functions
Read: EJS Ch.3 (Functions)
Quiz: What's a closure? What does "functions are values" mean in JS?
Problems: Write a function that returns a function (counter closure). Refactor a nested if-chain into early returns.

Day 4: Data structures: objects & arrays
Read: EJS Ch.4 (Data Structures)
Quiz: Difference between array and object destructuring? What does .map() return vs .forEach()?
Problems: Given an array of user objects, filter + map to get names of users over 18. Write a function to deep-clone a simple object without structuredClone.

Day 5: Higher-order functions
Read: EJS Ch.5 (Higher-Order Functions)
Quiz: What makes a function "higher-order"? Explain .reduce() in one sentence.
Problems: Use .reduce() to count word frequency in a sentence. Chain .filter().map().reduce() on a dataset.


Now, Let's start with the day 1

Day 1:  Values, types, operators
Read: EJS Ch.1 (Values, Types, Operators)
Quiz: What's the difference between null and undefined?
-> In JavaScript, undefined means a variable has been declared but has not yet been assigned a value, while null is an assignment value that represents the intentional absence of any object value. Think of undefined as a missing value that JavaScript sets automatically, whereas null is an empty value that you, the developer, set on purpose

What does NaN === NaN return?
 

What's short-circuit evaluation?


Problems: Write a function checking if a number is even/odd without %. Predict output of 5 type-coercion snippets (e.g. "5" + 3, "5" - 3).




Set up this TIL repo: dated entries with tag front matter, a `til.sh` helper
to scaffold new entries, and a pre-commit hook that regenerates the README
index automatically so I never have to update it by hand.

## Notes / details

- `./til.sh new` creates and opens today's file.
- Committing regenerates `README.md` via the `.githooks/pre-commit` hook.
- Multiple commits/pushes per day are fine — the log is just append-only files.

## Resources


- 
